name: First Hello World Workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  hello-world:
    runs-on: ubuntu-latest

    environment: Development

    steps:
      - name: Print Names
        run: |
          echo "List: ${{ vars.Name }}"
